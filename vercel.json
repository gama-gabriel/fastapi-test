{
	"installCommand": "echo oi && python3.12 -m venv env && source env/bin/activate && pip3.12 install -r --disable-pip-version-check --no-chache-dir requirements.txt",
	"buildCommand": "",
	"outputDirectory": ".",
	"builds": [
		{
	      "src": "app/index.py",
		  "use": "@vercel/static-build"
	    }
	],
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "app/index.py"
		}
	]
}
